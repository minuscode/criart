<p><%= @user.email %>,</p>

<p>A sua encomenda será enviada para:</p>
<span><%= @order.address %></span>

<p>Detalhes da sua encomenda</p>
<table>
    <thead>
        <tr>
            <th>Produto</th>
            <th>Quantidade</th>
            <th>Preço</th>
        </tr>
    </thead>
    <tbody>
        <% @order.line_items.each do |line| %>
        <tr>
            <td><%= line.product.name %></td>
            <td><%= line.quantity %></td>
            <td><%= line.product.price %></td>
        </tr>
        <% end %>
    </tbody>
    <tr colpan="3"><td>Total<td></tr>
</table>

<p>Obrigado pela sua encomenda!</p>

<p><%= link_to "Criart", root_path %></p>