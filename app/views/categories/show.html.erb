<p id="notice"><%= notice %></p>
<% session[:back] = request.fullpath %>

<h1><%=@category.name%></h1>
<div class="row">
<div class="span4">
	<div class="filters">
		<%= render "filters" %>
	</div>
</div>

<div class="span12">
	<div class="row">
		<%= render "products" %>
	</div>
	<div class="row">
		<%= paginate @products%>
	</div>
</div>
</div>
