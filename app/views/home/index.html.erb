<% if notice %>
<p id='notice'><%= notice %> </p>
<% end %>

<div>
	<% @categories.each do |category|%>
	<%= link_to image_tag(category.logo, :height => '320', :width => '320', :alt => category.name), category_path(category)%>
	<% end %>
</div>